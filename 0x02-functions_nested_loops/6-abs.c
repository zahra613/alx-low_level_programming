#include "main.h"
#include <stdio.h>

/**
* main - abs() of numbers
* written by black
* Return: Always 0.
*/
int _abs(int)
	{
    int n;
    n = _abs(n);
    _putchar(n+'0');
     _ putchar('\n');
      return(0);
}

