#include "main.h"
/**
* function that checks for lowercase character  Returns 1 if c is lowercase
Returns 0 otherwise
*
*   written by black
*
**/
int _islower(int c)
{ 
if(c='a'&& c<='z')
{
return(1);      
}
else {
return(0);
}
_putchar('\n');
return(0);
}	
