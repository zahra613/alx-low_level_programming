#include "main.h"
/**
* main - print alphabets in lower case and uppercase
* written by black
* Return: Always 0.
*/
int _isalpha(int c)
{
if (c >= 65 && c <= 122)     
return (1);
else
return (0);
}
