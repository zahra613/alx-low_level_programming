#include "main.h"
#include <stdio.h>
/**
* add  - Entry point
* @c: the charachter in ASCII code
* @z: the charachter in ASCII code
*  descreption: 'sum of wo integer'
*  Return sum: "print sum ".
*   written by black
* Return:  always 0 (success)
**/
int add(int z, int c)
{
int sum;
sum = z + c;
return (sum);
}
